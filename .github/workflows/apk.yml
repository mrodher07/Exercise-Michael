# Compila el APK de Android y lo deja descargable.
#
# Existe por una razón muy concreta: para instalar la aplicación en un móvil hace falta un
# APK, y compilarlo pide el SDK de Android (unos gigas, y `dl.google.com` no siempre está
# accesible según dónde se trabaje). Aquí lo hace GitHub, que ya trae el SDK instalado en sus
# máquinas.
#
# El APK sale por dos caminos, y el bueno para el móvil es el primero:
#
#   · Como Release de la etiqueta «apk», con enlaces fijos que siempre traen el último:
#     .../releases/latest/download/fitlog-arm64.apk   ← el bueno, unos 20 MB
#     .../releases/latest/download/fitlog-arm32.apk   ← teléfonos muy viejos
#     .../releases/latest/download/fitlog.apk         ← las tres arquitecturas, 51 MB
#     Se tocan en el móvil y se instalan, sin descomprimir nada y sin caducar.
#   · Como artefacto del propio flujo, que es un zip y caduca a los 90 días. Se queda por si
#     hace falta el APK de una compilación concreta.
#
# Que ese enlace funcione desde el móvil depende de una cosa: **este repositorio es público**.
# Mientras fue privado, la descarga iba autenticada y el gestor de descargas de Android se
# atascaba —la barra llegaba al 100 % y la descarga no terminaba nunca— porque el archivo
# viaja detrás de una redirección a otro dominio y ese gestor no siempre lleva la sesión. Si
# algún día el repositorio vuelve a ser privado, el APK habrá que publicarlo en algún sitio
# público o pasarlo al móvil a mano.
#
# Para las siguientes veces basta con instalar el APK nuevo encima: los datos se conservan
# porque van en la carpeta de la aplicación, no en el APK, y porque todos los APK que salen de
# aquí se firman con la misma clave (`movil/android/app/fitlog.jks`). Con una clave distinta en
# cada compilación —que es lo que pasa con la de depuración, que se genera nueva en cada
# máquina— Android trataría el APK nuevo como otra aplicación y no lo dejaría instalar encima.

name: APK de Android

on:
  push:
    branches: ['**']
    paths:
      - 'movil/**'
      - '.github/workflows/apk.yml'
  # Y a mano, cuando quieras un APK sin haber tocado nada.
  workflow_dispatch:

# Una compilación a la vez, y si llega otra se cancela la que estaba.
#
# Todas las ejecuciones escriben en el **mismo** archivo del mismo Release, así que dos a la
# vez —lo que pasa, por ejemplo, al empujar el mismo commit a una rama y a `main`— pueden
# pisarse subiendo el APK. Además la que se cancela no aporta nada: la que la adelanta trae
# ese commit o uno más nuevo.
concurrency:
  group: apk
  cancel-in-progress: true

jobs:
  compilar:
    runs-on: ubuntu-latest
    # Para poder publicar el Release. Sin esto el permiso por defecto puede ser de sólo
    # lectura y `gh release` falla con un 403.
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Dependencias
        working-directory: movil
        run: flutter pub get

      # El análisis y las pruebas van antes de compilar: si algo está roto, mejor saberlo
      # aquí que al abrir la aplicación en el móvil.
      - name: Analizar
        working-directory: movil
        run: flutter analyze

      - name: Pruebas
        working-directory: movil
        run: flutter test

      - name: Compilar el APK
        working-directory: movil
        run: flutter build apk --release

      # Y además uno por arquitectura.
      #
      # El APK de arriba es «universal»: lleva dentro el código nativo de las tres
      # arquitecturas de Android y por eso pesa 51 MB, de los cuales el móvil usa un tercio.
      # El de arm64 ronda los 20 MB y vale para cualquier teléfono de los últimos años.
      #
      # No es una cuestión de elegancia: una descarga de 51 MB tarda el triple y da el triple
      # de ocasiones a que el gestor de descargas de Android se quede colgado al final —que es
      # exactamente lo que estaba pasando—. El universal se sigue publicando por si algún día
      # hace falta un móvil viejo o raro.
      - name: Compilar un APK por arquitectura
        working-directory: movil
        run: flutter build apk --release --split-per-abi

      # El camino bueno para instalarlo en el móvil: un Release con el APK suelto dentro.
      #
      # Es **un solo Release** que se va reescribiendo, con la etiqueta fija «apk», y no uno
      # por compilación. Así el enlace `/releases/latest/download/fitlog-arm64.apk` siempre
      # trae el último APK y se puede guardar en favoritos del móvil, en vez de tener que
      # buscar cada vez cuál es la última ejecución. El historial de versiones ya está en los commits; una
      # lista de APK viejos no le sirve a nadie.
      #
      # Se publica después de analizar, pasar las pruebas y compilar, así que lo que hay ahí
      # colgado nunca es una versión que no arranque. Ojo con esto: cualquier rama que toque
      # `movil/` y llegue verde hasta aquí reescribe el APK del Release.
      #
      # El `github.token` del propio flujo basta porque el Release es de este repositorio.
      - name: Publicar el APK en el Release «apk»
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          # Se renombran: el nombre del archivo es la mitad del enlace fijo, así que tiene
          # que ser siempre el mismo.
          salida=movil/build/app/outputs/flutter-apk
          cp "$salida/app-arm64-v8a-release.apk"   fitlog-arm64.apk
          cp "$salida/app-armeabi-v7a-release.apk" fitlog-arm32.apk
          cp "$salida/app-release.apk"             fitlog.apk
          notas=$(cat <<TEXTO
          APK de FitLog compilado desde \`${GITHUB_SHA:0:7}\` el $(date -u '+%d/%m/%Y a las %H:%M UTC').

          ### Cuál descargar

          **[fitlog-arm64.apk](https://github.com/${GITHUB_REPOSITORY}/releases/latest/download/fitlog-arm64.apk) — éste.** Unos 20 MB, y vale para cualquier móvil de los últimos años.

          Los otros dos son para casos raros: \`fitlog-arm32.apk\` para un teléfono muy viejo, y \`fitlog.apk\` con las tres arquitecturas dentro (51 MB) si ninguno de los otros instala.

          Estos enlaces siempre traen el último APK compilado, así que valen igual para instalar y para actualizar.

          ### Instalar
          1. Toca el enlace en el móvil y descarga el archivo. Mejor desde Chrome o Firefox que
             desde un navegador metido dentro de otra aplicación: sus descargas se quedan
             colgadas al terminar.
          2. Abre el archivo. Android pedirá permiso para instalar aplicaciones de fuera de
             Play Store: se lo das a la aplicación desde la que lo abras.
          3. Si sale Play Protect, «Instalar de todas formas». Es tu propia aplicación.

          Para actualizar, instala el APK nuevo encima: **los entrenos no se van**, porque
          viven en la carpeta de la aplicación y todos los APK van firmados con la misma clave.
          TEXTO
          )
          if gh release view apk >/dev/null 2>&1; then
            # Se actualizan las notas y se pisa el archivo, en vez de borrar el Release y
            # crearlo otra vez: así el enlace de descarga nunca se queda sin APK detrás, ni
            # medio segundo. La etiqueta «apk» sigue apuntando al commit donde se creó —una
            # etiqueta que ya existe no se mueve—, y por eso el commit de verdad va escrito
            # en las notas.
            gh release edit apk --notes "$notas"
            gh release upload apk fitlog-arm64.apk fitlog-arm32.apk fitlog.apk --clobber
          else
            gh release create apk fitlog-arm64.apk fitlog-arm32.apk fitlog.apk \
              --title 'FitLog · último APK' \
              --notes "$notas" \
              --target "$GITHUB_SHA"
          fi

      # Y el mismo APK como artefacto del flujo, por si algún día hace falta el de una
      # compilación concreta y no el último.
      - name: Dejar el APK descargable también aquí
        # Aunque haya fallado la publicación del Release: si el camino bueno se rompe, el
        # respaldo es cuando más falta hace, y sin esto el paso se saltaba justo entonces. En
        # una ejecución cancelada sí se salta, que ahí no hay nada que guardar.
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: fitlog-apk
          path: movil/build/app/outputs/flutter-apk/app-release.apk
          # Noventa días es lo máximo que guarda GitHub; de todas formas, con volver a
          # lanzar la acción se genera otro.
          retention-days: 90
