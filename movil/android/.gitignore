gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Menos ésta, que sí entra en el repositorio y a propósito: es la que hace que un APK nuevo
# se instale encima del anterior sin borrar los datos. El razonamiento completo, y qué habría
# que cambiar si esto llegara a publicarse, está en app/build.gradle.kts.
!app/fitlog.jks
